# help and version

Use Clap's built-in help and version flags:

```bash
rescope --help
rescope --version
rescope snapshot --help
rescope live --help
rescope record --help
```

The help output lists current options and accepted enum values.
